#include "router.h"
#include <iostream>
#include <string>

int main()
{
    Router router;
    std::string pathRoutersFolder = "Router_Pages";
    router.autoSetUp(router, pathRoutersFolder);
    while (true)
    {

    }
    return 0;
}
